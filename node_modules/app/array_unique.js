module.exports = function (obj) {
  var o=[];
  var o2=[];
  obj.forEach(function(x) {
    var x2=JSON.stringify(x);
    if(o2.indexOf(x2)==-1) {
      o.push(x);
      o2.push(x2);
    }
  });
  return o;
}

